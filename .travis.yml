language: node_js

sudo: false

cache:
  apt: true
  directories:
    - node_modules

node_js:
  - "7"

before_script:
  - npm install
  - npm install -g hexo-cli
  - npm install hexo-deployer-git --save

script: 
  - hexo server
  - curl http://localhost:4000/
